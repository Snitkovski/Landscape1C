# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

> **Сначала прочитай [`docs/TZ.md`](docs/TZ.md).** Это главный документ проекта — техническое задание, замысел, принятые архитектурные решения и планы (две оси, шкала зрелости, связи между технологиями и граф, экспертный совет, слой компетенций, опрос популярности). Перед любыми содержательными изменениями сверяйся с ним и держи его в актуальном состоянии. Правила разметки осей (как присваивать роль/контекст/зрелость и т.д.) — в [`docs/METHODOLOGY.md`](docs/METHODOLOGY.md).

## О проекте

Интерактивный ландшафт (карта) технологий экосистемы 1С — наследник линейного каталога [StackTechnologies1C](https://github.com/Oxotka/StackTechnologies1C). Ключевая идея — навигация по **двум независимым осям**: функциональная роль и контекст работы. UI **только русский** — все метки, значения фильтров и контент пишутся по-русски.

## Архитектура

**Живой сайт — это `app/`: статика без сборки** (HTML/CSS/JS, без бандлера, без зависимостей).

- `app/data.js` — единственный источник данных сайта: глобальный объект `window.LANDSCAPE` (не `fetch`, чтобы работало и через `file://`, и на GitHub Pages). Содержит `categories`, `blocks` (группировка категорий), `axes` (определения фильтров) и `items` (карточки).
- `app/shared.js` — общие хелперы страниц с данными (`window.LandscapeUI`): `wbr` (точки переноса в длинных именах), `logoMarkup` и `sortItems` — единый порядок карточек внутри категории для главной и схемы. Здесь же `window.LandscapeFilters` (`read`/`patch`) — **общий стор отборов в localStorage** (`landscapeFilters`): роль/контекст/зрелость/… переносятся между главной, путём, схемой и графом. Каждая страница читает свои оси и патчит только их (остальные сохраняются — перенос работает туда-обратно); **пустой набор оси = «все»** (единая семантика везде). Подключается до `detail.js` на index/graph/scheme/path.
- `app/app.js` — главная страница: рендер фильтров и доски, фильтрация, сортировка, синхронизация состояния с URL (`?role=…&context=…&q=…`) для шаринга.
- `app/detail.js` — общий модуль детальной модалки (`window.openDetail(item)`), переиспользуется и главной, и графом. Здесь же блоки «Аналоги»/«Зависимости» и кнопки связей.
- `app/graph.html` + `app/graph.js` — отдельная страница «Граф связей»: force-directed canvas-граф по `analogs`/`depends`, клик по узлу открывает ту же модалку.
- `app/scheme.html` + `app/scheme.js` — страница «Схема»: статичный постер-древо (блок → категория → карточки инструментов) целиком в одном инлайн-SVG. Отбор по роли/зрелости и скрытие блоков (чекбоксы «Блоки»), клик по карточке открывает ту же `openDetail`-модалку. Экспорт SVG/PNG/PDF/майндмап (.mm) — без зависимостей: PNG/PDF через canvas (PDF = JPEG в минимальном PDF), .mm = FreeMind XML из того же `currentTree()`. Логотипы вшиваются в SVG (векторные — инлайн с `clipPath` по `viewBox`, чтобы открывалось в Illustrator; растровые — data-URI). На мобильном отборы прячутся в попап «Отборы» (триггер в шапке), «Скачать» → иконка «Поделиться».
- `app/path.html` + `app/path.js` — страница «Путь»: маршрут освоения инструментов для роли (ступени = шкала зрелости, группы = блоки ландшафта), отметки «знаю» с прогрессом в localStorage (`pathKnown`), роли в URL (`?role=…`, можно несколько через запятую; пустой выбор = все роли), клик по инструменту открывает общую модалку. Роль(и) синхронизируются с общим стором отборов (`LandscapeFilters`).
- `app/council.html` — экспертный совет (кураторы + автор, фото в `app/people/`); `app/methodology.html` — методология разметки.
- `app/nav.js` — общая «обвязка» всех страниц: из единого конфига (список ссылок в начале файла) генерирует бургер-кнопку (вставляется в `.topbar` и `.masthead`), выпадающее меню (навигация + Поддержать/Telegram + выбор темы светлая/темная/системная) и ссылки подвала; здесь же общая логика прилепленной шапки/подвала при прокрутке. Править ссылки нужно только здесь. Тема выбирается только в бургер-меню. Страничным скриптам отдает API `window.NAV`: `closeMenu()`, `pageActions(nodes)` (пункты текущей страницы в начале бургера — «Отборы» на главной, «Скачать» на схеме) и `dismissOnOutside(isOpen, keep, close)` (закрытие попапа по нажатию вне; гасит следующий click, чтобы не сработала карточка под попапом).
- `app/onboarding.js` — онбординг главной: цепочка тултипов при первом визите (только десктоп; флаг `onboarding_done` в localStorage). `app/404.html` — страница «не нашлась», GitHub Pages подхватывает её автоматически.
- `app/styles.css`, `app/index.html`, `app/logos/` — оформление и логотипы (SVG, имя файла указывается в `item.logo`; `null` → заглушка «1С»).

**Модель фильтрации (`app/app.js`).** Шесть осей: `role`, `context`, `maturity`, `origin`, `license`, `availability`. `role` и `context` в данных — массивы (`roles`, `contexts`), остальные — скаляры (`maturity`, `origin`, `license`, `availability`). Внутри одной оси выбранные значения объединяются по ИЛИ, между осями — по И. Курируемых «стартовых наборов» (kit-кнопок) больше нет — их роль выполняет фильтр по контексту.

**Инвариант значений.** Строковые значения в `item` (например `maturity: "продвинутое"`, `roles: ["devops"]`) должны точно совпадать со значениями в `axes.<ось>.values` в `app/data.js` — иначе фильтр/чип не сработает. При добавлении нового значения метки правь и `axes`, и элементы.

## Источник данных

Единственный источник — `app/data.js` (`window.LANDSCAPE`, поля `roles/contexts/maturity/origin/license/availability/why/start/analogs/depends/aliases/added/updated/...`). Правки контента вносить сюда — вручную или через редактор (`app/editor.html`, см. ниже).

**`added`/`updated`** — даты карточки в ISO (`YYYY-MM-DD`). `added` — когда добавлена, `updated` — последняя правка. Редактор штампует `updated` при любом изменении карточки (для двусторонних аналогов — и парной), новые карточки получают `updated` = дате создания. **При ручной правке `data.js` обнови `updated` сам** — `validate.js` требует корректные даты у каждой карточки и `updated >= added`. Поле пока только собирается, в UI не показывается (задел под историю изменений).

**`aliases`** — скрытый от пользователя массив поисковых синонимов (русские транслитерации англоязычных названий и пр.: `git`→`гит`, `SonarQube`→`сонаркуб`). Не отображается на карточке, участвует только в поиске (`app.js` → `matches()` подмешивает его в строку поиска). Редактируется в форме редактора (поле «Синонимы для поиска», через запятую).

### Где «зашиты» количества (инструменты / категории / блоки / связи)

Фактические числа считаются из `app/data.js`:

```bash
node -e 'global.window={};require("./app/data.js");const L=window.LANDSCAPE;
console.log("инструменты:",L.items.length,"| категории:",L.categories.length,"| блоки:",L.blocks.length,
"| аналоги:",L.items.filter(i=>i.analogs&&i.analogs.length).length,
"| зависимости:",L.items.filter(i=>i.depends&&i.depends.length).length)'
```

После добавления/удаления карточек обнови **ручные** упоминания (их легко забыть):

- `README.md` — «N+ инструментов в M категориях и K блоках» (round-хедж «200+» можно не трогать на каждой карточке, но категории/блоки держи точными).
- `docs/TZ.md` — число категорий/блоков (раздел «Категории и блоки») и «примерно у N карточек аналоги / M зависимости».

**Автоматические** (не правь руками, прогони генератор):

- `app/llms.txt` — число инструментов генерит `node scripts/sitegen.js` из `data.js`.
- `app/index.html` `#count`/`#count2`, `cat__num` в категориях и декоративный счётчик `.masthead__num` в шапке — проставляет `app.js` в рантайме (в HTML у `.masthead__num` стоит заведомо ненастоящий плейсхолдер `999` до загрузки JS — **не менять**).

## Команды

Все локально одним действием — `./start.command` (двойной клик в Finder или из терминала; на Windows — `start.cmd`): поднимает `scripts/serve.py` и открывает редактор в браузере. Если сервер уже работает — просто открывает вкладку. Зависимости — только системные Python 3 и Node.js, пакетов нет (`pip`/`npm install` не нужны); без Node сайт и редактор работают, но «Сохранить» не прогонит cachebust/sitegen, а «Собрать dist» вернет понятную ошибку.

Локальный предпросмотр без записи (сборки нет):

```bash
cd app && python3 -m http.server 8123
# http://127.0.0.1:8123/
```

Редактор разметки (`app/editor.html`) с прямой записью в `app/data.js` по кнопке «Сохранить» — нужен пишущий сервер (`python3 -m http.server` записывать не умеет):

```bash
python3 scripts/serve.py   # его и запускает start.command
# http://127.0.0.1:8123/editor.html
# POST /save  → пишет app/data.js и сам прогоняет cachebust
# POST /build → сборка dist/ кнопкой «Собрать dist» (build.js сам гоняет validate, sitegen, cachebust)
```

`editor.js` сериализует весь `window.LANDSCAPE` (включая `updated`, `blocks`, все 6 осей) обратно тем же 2-пробельным JSON; при сохранении дата `updated` ставится автоматически. Без сервера «Сохранить» откатывается на скачивание копии.

Проверка целостности данных (оси, блоки, файлы логотипов, связи и симметрия аналогов, дубли имен, канон-форма файла, буква «ё», даты added/updated):

```bash
node scripts/validate.js
```

Проверка внешних ссылок карточек (homepage/repo/start) на живость — вручную, время от времени; в CI не гоняется, 403 у ботов — норма, результат проверяй руками:

```bash
node scripts/linkcheck.js
```

Sitemap и llms.txt (`app/sitemap.xml`, `app/llms.txt` — каталог для поисковиков и ИИ-ассистентов) генерируются из `app/data.js`; прогонять после содержательных правок данных:

```bash
node scripts/sitegen.js
```

Cache-bust: проставить версии `?v=<хеш>` всем локальным ассетам (css/js) во всех `app/*.html` по содержимому файла. Заменяет ручной бамп `?v=` на каждой странице — поправил файл, прогнал команду, версии обновились только у изменившихся ассетов сразу везде:

```bash
node scripts/cachebust.js
```

Сборка `dist/` для ручной выгрузки на хостинг (прогоняет валидатор, sitegen и cachebust, затем копирует публичную статику из `app/`, выкидывая редактор `editor.*` и `.DS_Store`). Из терминала или кнопкой «Собрать dist» в редакторе:

```bash
node scripts/build.js
```

Деплой — GitHub Actions (`.github/workflows/deploy.yml`): собирает `dist/` (`node scripts/build.js` — валидация + sitegen + cachebust + копия `app/` без редактора) и выкладывает его на GitHub Pages. **Автодеплой на каждый push в `main`** (плюс ручной запуск `workflow_dispatch`) — то есть push в `main` сразу публикует сайт на **https://landscape1c.ru**. Пути в `app/` относительные, домен задан в Settings → Pages.

## Соглашения

- Юнит-тестов нет; целостность данных проверяет `node scripts/validate.js`, вёрстка — визуально через локальный предпросмотр.
- Краткое описание (`description`) и «зачем нужно» (`why`) не заканчиваются точкой — последнее предложение пишется без точки. Это правило проверяет `validate.js`.
- Тёмные (монохромные) логотипы помечаются `"logoInvert": true` в `app/data.js` — движок сам инвертирует их в тёмной теме (класс `is-invert`).
- Стиль кода в `app/app.js` лаконичный: короткие хелперы (`$`, общие — из `window.LandscapeUI`), секции под `// ──` комментариями. Держись этого стиля.
- Форматирование — Prettier (`.prettierrc`, `tabWidth: 4`); `app/*.{html,css,js}` прогнаны через него. После правок прогоняй `npx prettier --write app/<файл>`, чтобы не плодить шумные диффы. Исключение — `app/data.js` (в `.prettierignore`): его пишет редактор 2-пробельным JSON, Prettier его не трогает; канон-форму файла сторожит `validate.js`.
- Контентные страницы (схема, методология, экспертный совет) помечены `class="page-doc"` на `<body>`: на мобильном (`@media max-width 720px`) большой `masthead` скрыт, прилепленная шапка (`.topbar`) показана сразу, подвал не показывается. Главная (`class="page-main"`) ведёт себя так же (подвал на мобильном тоже скрыт); сайдбар фильтров на мобильном прячется и открывается попапом через пункт «Отборы», который `app.js` вставляет в бургер-меню (как `scheme.js` вставляет «Скачать»). Граф — свой паттерн (`page-graph`, иммерсив + триггер «Отборы» в шапке).
- Cache-bust на GitHub Pages: версии `?v=` у ассетов проставляются хешем содержимого скриптом `node scripts/cachebust.js` (не правь `?v=` вручную) — прогоняй после правок css/js/data.js. Исключение — новый файл: скрипт обновляет только уже существующие `?v=`, поэтому у нового ассета один раз проставь заглушку `?v=0` и прогони скрипт.
- Связи в редакторе (`editor.js`): **`analogs` двусторонние** (ставятся/снимаются с обеих карточек), **`depends` односторонние** (только у текущей). Симметрию аналогов при ручной правке `data.js` проверит `validate.js`.
